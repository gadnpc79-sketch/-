# 📱 스마트폰에서 앱 사용하기

이 앱을 스마트폰에서 사용하는 방법을 안내합니다.

## 🌐 온라인 배포 (권장)

### Vercel을 사용한 무료 배포

1. **Vercel CLI 설치** (처음 한 번만)
   ```bash
   npm install -g vercel
   ```

2. **배포하기**
   ```bash
   vercel
   ```
   - 처음 실행 시 Vercel 계정 로그인 필요 (GitHub, GitLab, Email 중 선택)
   - 프로젝트 설정은 모두 기본값(Enter)으로 진행
   - 배포가 완료되면 URL이 제공됩니다 (예: https://your-app.vercel.app)

3. **스마트폰에서 접속**
   - 제공된 URL을 스마트폰 브라우저에서 열기
   - 홈 화면에 추가하여 앱처럼 사용 가능

### 프로덕션 배포
```bash
vercel --prod
```

## 📡 로컬 네트워크 접근 (개발용)

같은 Wi-Fi 네트워크에서만 사용 가능합니다.

1. **서버 실행**
   ```bash
   npm run dev
   ```

2. **로컬 IP 확인**
   - Windows: `ipconfig` 명령어로 IPv4 주소 확인
   - 터미널에 표시된 Network URL 확인

3. **스마트폰에서 접속**
   - 컴퓨터와 같은 Wi-Fi에 연결
   - 스마트폰 브라우저에서 `http://[IP주소]:5175` 접속
   - 예: `http://192.168.0.10:5175`

## 🔒 중요 사항

### GPS 권한
- 송신자 모드에서 위치 정보를 사용하려면 GPS 권한 허용 필요
- 브라우저에서 위치 권한 요청 시 "허용" 선택

### HTTPS 요구사항
- GPS 기능은 HTTPS 환경에서만 작동
- Vercel 배포 시 자동으로 HTTPS 제공
- 로컬 네트워크에서는 GPS 기능이 제한될 수 있음

### 홈 화면에 추가

**iOS (Safari):**
1. 공유 버튼 클릭
2. "홈 화면에 추가" 선택
3. 앱 이름 입력 후 추가

**Android (Chrome):**
1. 메뉴(⋮) 클릭
2. "홈 화면에 추가" 선택
3. 앱 이름 입력 후 추가

## 🎯 배포 후 관리

### 업데이트
코드 수정 후 다시 배포:
```bash
vercel --prod
```

### 배포 내역 확인
```bash
vercel ls
```

### 도메인 관리
Vercel 대시보드(https://vercel.com)에서:
- 커스텀 도메인 연결 가능
- 배포 내역 및 로그 확인 가능
- 환경 변수 설정 가능
